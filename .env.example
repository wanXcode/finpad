# FinPad Environment Configuration
# Copy to .env and fill in values

# Security
SECRET_KEY=change-me-to-random-string

# AI Analysis (optional, falls back to local analysis if empty)
AI_API_KEY=
AI_API_BASE=https://api.5666.net/v1
AI_MODEL=gpt-4o

# Default admin (only used on first startup)
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=finpad2026
